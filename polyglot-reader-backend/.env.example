## Backend environment variables (example)
PORT=3000
JWT_SECRET=replace_me

## Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

## Translation provider
## Pick one:
## - google (recommended for broad language coverage incl. Bengali)
## - libretranslate (self-host / hosted instance)
## If omitted, provider auto-detects based on keys below.
TRANSLATION_PROVIDER=google

## Google Cloud Translation API (v2) - simplest REST key
GOOGLE_TRANSLATE_API_KEY=

## LibreTranslate
LIBRETRANSLATE_URL=
LIBRETRANSLATE_API_KEY=

## Book summary rewrite (LLM)
## Used by /api/summaries/rewrite to produce a short single-paragraph summary in the learner's chosen language.
## If Mistral is not configured, the backend falls back to the translation provider above.
MISTRAL_API_KEY=
## If you created a Mistral Agent in Mistral Studio, put its id here (looks like: ag_...).
## When set, /api/summaries/rewrite will use the Agents API (POST /v1/conversations) instead of raw chat completions.
MISTRAL_AGENT_ID=
MISTRAL_MODEL=mistral-large-latest
MISTRAL_TEMPERATURE=0.5
MISTRAL_MAX_TOKENS=280
MISTRAL_TOP_P=1
MISTRAL_TIMEOUT_MS=9000

## Quotes provider (used for daily quote-of-the-day)
## Default: FavQs Quote of the Day (reachable in more networks). Used server-side; no client keys.
QUOTE_SOURCE_PROVIDER=favqs_qotd

## FavQs Quote of the Day
FAVQS_QOTD_URL=https://favqs.com/api/qotd

## Quotable (may be blocked on some DNS)
QUOTABLE_API_BASE=https://api.quotable.io
QUOTABLE_TAGS=education|learning|books|reading|literature|wisdom
QUOTABLE_MAX_LENGTH=140

## ---------------------------------------------------------------------------
## Ebook catalog sync (DB-backed catalog: ebook_catalog_items)
##
## Run manually:
##   npm run sync:ebooks -- --lang=en
##   npm run sync:ebooks -- --all
##
## Or run automatically inside the API process:
##   EBOOK_CATALOG_AUTO_SYNC=true
## ---------------------------------------------------------------------------
EBOOK_CATALOG_AUTO_SYNC=false
EBOOK_CATALOG_LANGS=en,de
EBOOK_CATALOG_SYNC_INTERVAL_HOURS=24

## Per-source limits (optional tuning)
EBOOK_CATALOG_GUTENDEX_LIMIT=1200
EBOOK_CATALOG_GUTENDEX_MAX_PAGES=80
EBOOK_CATALOG_GUTENDEX_PAGE_DELAY_MS=150
EBOOK_CATALOG_WIKISOURCE_LIMIT=600
EBOOK_CATALOG_WIKISOURCE_ALLPAGES_LIMIT=3500

## ManyBooks is sometimes blocked server-side (Cloudflare). Set to 0 to disable.
EBOOK_CATALOG_MANYBOOKS_LIMIT=350

PORT=3000
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# Use ONE of these (service role key is preferred for backend):
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
JWT_SECRET=supersecretkey123

